# Head-unit-shaped canvas, so screens lay out the way the car lays them out rather than on a
# phone default. Today's assertions happen to pass either way.
qualifiers=w1280dp-h800dp-mdpi
# Plain Application: the module's real one is @HiltAndroidApp, and none of the DI it stands up is
# reachable from a screen rendered with its state passed in.
application=android.app.Application
