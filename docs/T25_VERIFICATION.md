# T25 — verification record

Covers the build checks and device pass for `docs/superpowers/plans/2026-09-14-t25-crashlytics.md`
(Tasks 1 and 3).

- **Date:** 2026-09-14
- **Branch:** `ek/t25-crashlytics`, wiring at `d5ebf1a`
- **Car AVD:** `AAOS_AOSP_33_userdebug`, driver user 10, `oem` build, version 1.0 (1)
- **Release APK for testing:** `automotive-oem-release-unsigned.apk` signed locally with `apksigner` and
  `~/.android/debug.keystore` (neither app has a release `signingConfig`)

## Build checks

Gate (Task 4): `./gradlew :app:assembleDebug :app:assembleRelease :automotive:assembleOemDebug
:automotive:assembleOemRelease :automotive:assemblePlaystoreDebug :automotive:assemblePlaystoreRelease
test detekt :app:lintDebug :app:lintRelease :core:data:lintDebug :automotive:lintOemDebug` — BUILD
SUCCESSFUL.

| Criterion | Result |
|---|---|
| `firebase-common` on `:app` `releaseRuntimeClasspath` | 21.0.0, before and after |
| `com.google.firebase.crashlytics.mapping_file_id` in each release APK (`:app`, `oem`, `playstore`) | present, `"00000000000000000000000000000000"` |
| `firebase_crashlytics_collection_enabled` in merged manifests | `false` in `debug`, `oemDebug`, `playstoreDebug`; absent from all three release variants |
| `<activity>` count per merged manifest | unchanged: release 7, debug 9, oemRelease 8, oemDebug 10, playstoreRelease 7, playstoreDebug 9 |
| `version_control_info` in each release APK | git SHA of the build commit, root `$PROJECT_DIR`; empty in debug |

The build ID is 32 zeros. It is not a missing ID: the plugin writes it into a generated resource
(`injectCrashlyticsMappingFileId*`, headed "automatically generated by Crashlytics"), and for a build
that is not minified there is no mapping file to identify. On the device the SDK logged
`Mapping file ID is: 00000000000000000000000000000000` and started; a missing ID stops it at launch
with "The Crashlytics build ID is missing".

## The car

| Step | Result |
|---|---|
| Release: launch, `am crash --user 10 com.example.nyasaplayer`, relaunch | **Sent.** `RemoteServiceException$CrashedByAdbException: shell-induced crash` on `main` reached Crashlytics' handler; enqueued at 19:26:06.053 IST and uploaded — see "Upload evidence" below |
| Debug: same crash, relaunch | **Not sent.** `Crashlytics automatic data collection DISABLED by firebase_crashlytics_collection_enabled manifest flag.`; the crash was handled — see "Debug evidence" below |

### Upload evidence

From `t25-car-upload.log` — the first line is the enqueue (`FirebaseCrashlytics`); the other two are
the HTTP upload, tag `TRuntime.CctTransportBackend`, Info level, visible with
`adb logcat -s FirebaseCrashlytics TRuntime.CctTransportBackend` and no `setprop` (the property was
unset when this was captured):

```
09-14 19:26:06.053 D/FirebaseCrashlytics(17819): Crashlytics report successfully enqueued to DataTransport: 6AA83C1C00EE0001459B4EB1B9598EBE
09-14 19:26:09.264 I/TRuntime.CctTransportBackend(17976): Making request to: https://crashlyticsreports-pa.googleapis.com/v1/firelog/legacy/batchlog
09-14 19:26:10.926 I/TRuntime.CctTransportBackend(17976): Status Code: 200
```

The release log's "Loaded cached settings" line includes `"collect_anrs":true` — Crashlytics 19.4.4
reports ANRs (from `ApplicationExitInfo` on API 30+) in this configuration, not just crashes; see
`docs/CRASH_REPORTING.md`.

### Debug evidence

From `t25-car-debug.log`, after the manifest flag disabled collection for the session:

```
09-14 19:28:04.885 18294 18318 D FirebaseCrashlytics: Automatic data collection is disabled.
09-14 19:28:04.886 18294 18318 D FirebaseCrashlytics: Waiting for send/deleteUnsentReports to be called.
```

No send, enqueue or upload line followed, including after relaunch.

`am crash` is enough to test with: it raises a Java exception on the main thread, so no `throw` was
committed.

The debug crash is kept on the device, unsent:
`files/.crashlytics.v3/com.example.nyasaplayer/priority-reports/6AA83C7F02A1000146CE9B898F544179`. A
build with collection on, run in the same data directory, sends it. That report was deleted, with
the owner's approval, before T26's release test on 2026-09-15.

Builds were swapped with `adb install -r` rather than uninstalled first, against the ticket's note.
It didn't affect the release result: the debug build it replaced was from 2026-09-09, before any build
had the SDK, so nothing could be cached, and the release run enqueued exactly one report.

## The dashboard

Checked by the owner through the Crashlytics reporting API on 2026-09-15, during T26
(`docs/T26_VERIFICATION.md` → "The dashboard"): the `CrashedByAdbException` event is there at
19:26:05 IST on 2026-09-14, and `topVersions` over 2026-09-14 and 2026-09-15 counts two events in
all, this one and T26's release crash. The debug crash never reached the dashboard.

## The phone — run 2026-09-19

`Medium_Phone_API_35` had refused the release build on 2026-09-14: `adb install -r` →
`INSTALL_FAILED_INSUFFICIENT_STORAGE` (`/data` 95% full, 316 MB free — T29's own attempt read
312 MB and a different message for the same block). The pass was re-run on `Pixel_9_Pro_Fold_API_35`
(`emulator-5558`, API 35, `/data` 37% used), signed in, against `:app` at `a0214b1`.

The release APK has no `signingConfig`, so `app-release-unsigned.apk` was signed locally with
`apksigner` and `~/.android/debug.keystore` — the same method the car used. Builds were swapped with
`adb install -r`, which keeps the signed-in data directory; the stored-report ambiguity that would
otherwise create was removed by hand, see below.

| Step | Result |
|---|---|
| One stale report from an earlier debug crash sat in `priority-reports` (`6AA9859A…`) | Deleted before the release install, with the owner's standing approval, so any upload could only be this run's |
| Release: install, launch (pid 7637), `am crash 7637`, relaunch (pid 7790) | **Sent.** `POST https://crashlyticsreports-pa.googleapis.com/v1/firelog/legacy/batchlog` → `Status Code: 200` at 12:21:58 IST, 5 s after relaunch |
| Debug: install, launch (pid 8008), `am crash 8008`, relaunch (pid 8130) | **Not sent.** No enqueue and no request to `crashlyticsreports-pa` after the crash or the relaunch |
| Debug crash stored instead | `priority-reports/6AAE70C900F600011F482AA5BF87E6E8`, written but never uploaded; deleted afterwards so the next release run in this data directory starts clean |
| Dashboard | `topVersions` over 2026-09-19 counts **one** event, version `1.0 (1)` — the release crash. The debug crash is not there |

The release upload, from the relaunch after the crash (`adb logcat -s FirebaseCrashlytics
TRuntime.CctTransportBackend`):

```
09-19 12:21:53.678  7790  7790 I FirebaseCrashlytics: Initializing Firebase Crashlytics 19.4.4 for com.example.nyasaplayer
09-19 12:21:58.121  7790  7805 I TRuntime.CctTransportBackend: Making request to: https://crashlyticsreports-pa.googleapis.com/v1/firelog/legacy/batchlog
09-19 12:21:58.291  7790  7805 I TRuntime.CctTransportBackend: Status Code: 200
```

The debug relaunch, same filter and a 30 s window after it — the SDK starts, and that is all:

```
09-19 12:24:21.586  8130  8130 I FirebaseCrashlytics: Initializing Firebase Crashlytics 19.4.4 for com.example.nyasaplayer
09-19 12:24:21.709  8130  8149 I FirebaseCrashlytics: Saved version control info
```

`topIssues` returns nothing for this app over the last seven days, including the 2026-09-14 and
2026-09-15 car events this record already shows are on the dashboard: `topIssues` omits closed
issues. `topVersions` counts events either way, which is why it is the check used here.

The enqueue line the car pass quotes (`Crashlytics report successfully enqueued to DataTransport:
<id>`, D-level) did not appear in either phone log. The upload did, at Info level with a 200, and
the dashboard has the event — the evidence stands on those two rather than on the enqueue line.

`am crash <pid>` again: the package form is a no-op on API 35 (`docs/T26_VERIFICATION.md`).
