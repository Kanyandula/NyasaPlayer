<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <service
            android:name="com.example.nyasaplayer.core.playback.PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService"/>
                <!-- AAOS enumerates media sources by the legacy action; Media3's
                     MediaSessionService.onBind already serves it. -->
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
            <!-- Opt-in the car media app checks before listing this as a media source;
                 without it the service is skipped as a "non media template app". -->
            <meta-data
                android:name="androidx.car.app.launchable"
                android:value="true" />
        </service>
    </application>
</manifest>
